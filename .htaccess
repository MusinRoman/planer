# Указываем серверу Apache использовать 404.php для обработки ошибок 404
ErrorDocument 404 /404.php

# Следующие строки могут быть полезны, если у вас есть проблемы с RewriteEngine
# или если вы хотите, чтобы все запросы перенаправлялись через index.php (фронт-контроллер)
# Если у вас уже есть RewriteRule для вашего MVC-фреймворка или SPA, этот блок может быть лишним.
# Пожалуйста, будьте осторожны при добавлении RewriteRule.

# Если вы хотите, чтобы все запросы, не являющиеся существующими файлами/директориями,
# перенаправлялись на index.php (это часто используется во фреймворках, но не для 404!)
# RewriteEngine On
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ index.php/$1 [L]